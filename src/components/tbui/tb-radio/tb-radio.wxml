<radio-group bindchange="onChange" style="display:inline-block;">
  <radio value="{{_value}}" disabled="{{disabled}}" checked="{{_checked}}" color="{{color}}" name="{{name}}"></radio>
  <text wx:if="{{label}}">{{label}}</text>
</radio-group>

