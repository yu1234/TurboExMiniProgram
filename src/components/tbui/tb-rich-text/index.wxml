<view style="padding:{{padding}}px;">
  <scroll-view scroll-y="{{true}}" scroll-x="{{true}}">
   <node nodes="{{_nodes}}" window-width="{{windowWidth}}" window-height="{{windowHeight}}" bind:tagATap="tagATap" bind:tagImgTap="tagImgTap"></node>
  </scroll-view>
</view>
